# include<stdio.h>
int main()
{
    // input array
    printf("Number of Inputs: ");
    int n;
    scanf("%d",&n);
    int arr1[n];
    for(int i=0;i<n;i++)
    {
        int a;
        scanf("%d",&a);
        arr1[i]=a;
    }
    return 0;

    // what and where to input
    printf("New Element\n jsn: ");


return 0;
}